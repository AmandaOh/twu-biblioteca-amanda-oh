Assumptions:
    - User is only able to check out one book at a time.
    - User is only able to return one book at a time.

    -Library Number is Unique (no two users can have the same library number)